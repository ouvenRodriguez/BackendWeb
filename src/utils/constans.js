const consts = {
  sv: { ctx: "[SERVER]" },
  ctxToken: { ctx: "[TOKEN]" },
  apiUser: "[ROUTE] [/api/user] ",
  apiProject: "[ROUTE] [/api/project] ",
};

export default consts;
export const { sv, ctxToken, apiUser, apiProject } = consts;
